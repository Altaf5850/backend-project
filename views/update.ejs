<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <div class="nav mb-10">
        <a class="text-blue-500" href="/read">Read Users</a>
      </div>
      <h1 class="text-3xl font-bold tracking-tighter mb-3">Update User</h1>
      <% if (user) { %>
      <form action="/update/<%= user._id %>" method="post">
        <input type="hidden" name="id" value="<%= user._id %>" />
        <input
          class="px-5 py-2 border-2 outline-none border-zinc-800 rounded-lg bg-transparent"
          type="text"
          name="name"
          placeholder="name"
          value="<%= user.name %>"
        />
        <input
          class="px-5 py-2 border-2 outline-none border-zinc-800 rounded-lg bg-transparent"
          type="email"
          name="email"
          placeholder="email"
          value="<%= user.email %>"
        />
        <input
          class="px-5 py-2 border-2 outline-none border-zinc-800 rounded-lg bg-transparent"
          type="text"
          name="imageURL"
          placeholder="imageURL"
          value="<%= user.imageURL %>"
        />
        <input
          type="submit"
          value="Update User"
          class="px-3 py-2 bg-green-500 text-black rounded-lg ml-3"
        />
      </form>
      <% } else { %>
      <p>No user selected for update.</p>
      <% } %>
    </div>
  </body>
</html>
